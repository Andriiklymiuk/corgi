# corgi suggest-history

## corgi suggest-history

Read/append the proactive-suggest dedupe + rate-limit state (.corgi/suggest-history.json)

### Synopsis

Thin state helper for the proactive-suggest skill. The state file is
per-developer audit data (gitignored) the skill reads to avoid re-filing an
idea that is already open or recently dismissed, and to enforce the weekly
filing cap. The skill never hand-rolls this JSON — it calls these subcommands.

  corgi suggest-history list   [--workspace `<p>]` [--json]
  corgi suggest-history check  --slug `<s>` [--cooldown 720h] [--max `<n>]` [--workspace `<p>]` [--json]
  corgi suggest-history record --slug `<s>` --status `<filed|dismissed|proposed|skipped>` [--ticket `<K>]` [--title `<t>]` [--lens `<l>]` [--workspace `<p>]` [--json]
  corgi suggest-history config [--json]

### Options

```
  -h, --help               help for suggest-history
      --workspace string   workspace root (default: cwd); cron passes an absolute path
```

### Options inherited from parent commands

```
      --describe                  Describe contents of corgi-compose file
      --dockerContext string      Specify docker context to use, can be default,orbctl,colima (default "default")
  -l, --exampleList               List examples to choose from. Click on any example to download it
  -f, --filename string           Custom filepath for for corgi-compose
      --fromScratch               Clean corgi_services folder before running
  -t, --fromTemplate string       Create corgi service from template url
      --fromTemplateName string   Create corgi service from template name and url
  -g, --global                    Use global path to one of the services
      --interactive               Force interactive prompts even when no TTY/agent detected
      --json                      Emit machine-readable JSON output
      --privateToken string       Private token for private repositories to download files
  -o, --runOnce                   Run corgi once and exit
      --silent                    Hide all welcome messages
```

### SEE ALSO

* [corgi](corgi)	 - Corgi cli magic friend
* [corgi suggest-history check](corgi_suggest-history_check)	 - Decide whether a candidate slug should be skipped (deduped or rate-limited)
* [corgi suggest-history config](corgi_suggest-history_config)	 - Echo the effective proactive-suggest mode (autoFileDrafts, maxPerWeek)
* [corgi suggest-history list](corgi_suggest-history_list)	 - List recorded proactive-suggest entries
* [corgi suggest-history record](corgi_suggest-history_record)	 - Append one proactive-suggest outcome entry

###### Auto generated by spf13/cobra on 8-Jun-2026
